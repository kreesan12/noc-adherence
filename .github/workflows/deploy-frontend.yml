name: Legacy Frontend Deploy (GitHub Pages Retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Frontend hosting retired from GitHub Pages
        run: |
          echo "GitHub Pages frontend deploys were retired on 2026-08-06."
          echo "Production frontend is now served from xneelo nginx on noc-api-01."
